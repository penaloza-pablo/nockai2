{
  "name": "fetch-guesty-bookings",
  "version": "1.0.0",
  "description": "Lambda function to fetch bookings from Guesty API",
  "main": "handler.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-amplify": "^6.6.6",
    "@aws-sdk/client-ssm": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
} 